name: Zizmor GitHub Actions static analysis
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  zizmor:
    name: Analyse with Zizmor

    permissions:
      actions: read
      contents: read
      # required by the reusable workflow's nested jobs (job-workflow-ref, grafana-bench)
      id-token: write
      # required to comment on pull requests with the results of the check
      pull-requests: write
      # required to upload the results to GitHub's code scanning service
      security-events: write

    uses: grafana/shared-workflows/.github/workflows/reusable-zizmor.yml@551bc8d50017d3e95d5da3f8a4826e733a208dc0 # main
    with:
      fail-severity: high
      min-severity: high
